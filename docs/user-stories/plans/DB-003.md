# Plan de Implementación: [DB-003] Esquema Base de Datos: Planos y Capas

**Estado:** Completado

Este documento detalla el plan para implementar la estructura de datos necesaria para la gestión de planos (Sheets) y capas (Layers), correspondiente a la User Story US-003.

## Objetivos
- Actualizar el esquema de Prisma (`schema.prisma`) para incluir los modelos `Plan` y `Layer`.
- Definir las relaciones entre proyectos, planos y capas.
- Generar y aplicar la migración en la base de datos PostgreSQL.

## Pasos de Implementación

### 1. Actualización de `schema.prisma`
Modificar el archivo `apps/backend/prisma/schema.prisma` para agregar los nuevos modelos y enumerados.

**Cambios propuestos:**
- **Enums:**
  - `PlanStatus`: DRAFT, ACTIVE, ARCHIVED.
  - `LayerType`: BASE, OVERLAY.

- **Modelos:**
  - `Plan`: Representa un plano (sheet) y sus versiones.
    - Campos: `id`, `project_id`, `sheet_name`, `version`, `status`.
    - Relación: Pertenece a `Project`. Tiene muchos `Layer`.
    - Restricción Única: `[projectId, sheetName, version]`.
  
  - `Layer`: Representa una capa (archivo de imagen) dentro de un plano.
    - Campos: `id`, `plan_id`, `name`, `image_url`, `type`, `status`.
    - Relación: Pertenece a `Plan`.
    - Cascade Delete: Si se borra el plan, se borran sus capas.

### 2. Generación de la Migración
Ejecutar el comando de Prisma para crear la migración SQL.

```bash
npx prisma migrate dev --name init_plans_layers
```

### 3. Verificación
- Verificar que el archivo de migración se ha creado correctamente en `prisma/migrations`.
- Comprobar que los tipos de TypeScript se han regenerado en `node_modules`.

## Referencias
- [US-003: Gestión de Planos y Carga de Capas](../US-003-CargaPlanos.md)
